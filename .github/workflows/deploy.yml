name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Login to Docker Hub
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

    - name: Build Backend Image
      run: docker build -t pavankamasani/backend ./backend

    - name: Push Backend Image
      run: docker push pavankamasani/backend

    - name: Build Frontend Image
      run: docker build -t pavankamasani/frontend ./frontend

    - name: Push Frontend Image
      run: docker push pavankamasani/frontend

    - name: Deploy to EC2
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ubuntu
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          docker pull pavankamasani/backend
          docker pull pavankamasani/frontend

          docker rm -f backend frontend || true

          docker run -d -p 5000:5000 --name backend pavankamasani/backend
          docker run -d -p 3000:80 --name frontend pavankamasani/frontend
